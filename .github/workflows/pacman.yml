name: Generate Pacman Graph

on:
  schedule: # Roda automaticamente todo dia à meia-noite
    - cron: "0 0 * * *"
  workflow_dispatch: # Permite que você rode manualmente agora

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Pacman Graph
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: recaihd
          # Opções: github, github-dark, ocean, midnight, etc.
          github_graph_theme: github-dark 
          
      - name: Push to Output
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}